import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  transpilePackages: ['asasvirtuais', 'asasvirtuais-airtable'],
  crossOrigin: 'anonymous',
};

export default nextConfig;
